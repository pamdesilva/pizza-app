import React from 'react';
import { Navbar } from 'react-bootstrap';

const Footer = () => (
  <Navbar fixedBottom>
  <Navbar.Header>
    <Navbar.Text>
      Baking fresh pizzas since 2018.
    </Navbar.Text>
  </Navbar.Header>
</Navbar>
);

export default Footer;
